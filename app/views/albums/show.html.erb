<p>
  <strong>Title:</strong>
  <%= @album.title %> Album
</p>
<%# .variant %>
<p>
  <div style='width: 500px; height:auto; margin:0 auto'>
    <% @imaage.each do |image| %>
      <%= image.id %>
      <% if image.image.attached!%>
        <%= image.image.image_url %>
      <% end %>
      <%# <%= image_tag(image.image, style:'width:100%') %> %>
    <% end %>
    <%# <%= image_tag(@album.imaages.variant, style:'width:100%') %> %>
  <div>
</p>

<p>
  <strong>User:</strong>
  <%= @album.user.email.split('@')[0].capitalize %>
</p>

<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Back', albums_path %>
